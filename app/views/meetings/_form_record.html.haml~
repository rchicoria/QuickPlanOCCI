- if @meeting.errors.any?
  #errorExplanation
    %h2= "#{pluralize(@meeting.errors.count, 'error')} prohibited this user from being saved:"
    %ul
    - @meeting.errors.full_messages.each do |msg|
      %li= msg

- count = 0

%table
  - count = 0
  = f.fields_for :topics  do |p|  
    -puts "ddddddddddddd"+count    
    .field
      %p{:class => 'fields'}
        = p.label :name, @meeting.topics.all[count].name
        %br
        = p.text_area :description
    - count += 1
//    = p.fields_for :tasks do |t|
//      %p{:class => 'fields'}
//        = t.label :name, "Task"
//        = t.text_field :description
//        = t.text_field :assignee
